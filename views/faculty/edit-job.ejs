<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Job - UMP Job Connect</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <div class="container">
      <a href="/" class="logo">UMP Job Connect</a>
      <ul>
        <li><a href="/faculty/dashboard">Dashboard</a></li>
        <li><a href="/faculty/jobs/create">Post Job</a></li>
        <li><a href="/faculty/profile">Profile</a></li>
        <li><a href="/auth/logout">Logout</a></li>
      </ul>
    </div>
  </nav>

  <main>
    <div class="container">
      <div class="card" style="max-width: 800px; margin: 0 auto;">
        <h1>Edit Job Post</h1>

        <% if (error) { %>
          <div class="alert alert-error"><%= error %></div>
        <% } %>

        <form action="/faculty/jobs/edit/<%= job.id %>" method="POST">
          <div class="form-group">
            <label>Job Title:</label>
            <input type="text" name="job_title" value="<%= job.job_title %>" required>
          </div>

          <div class="form-group">
            <label>Faculty:</label>
            <select name="faculty" required>
              <option value="">Select Faculty</option>
              <option value="Faculty of Agriculture and Environmental Sciences" 
                      <%= job.faculty === 'Faculty of Agriculture and Environmental Sciences' ? 'selected' : '' %>>
                Agriculture and Environmental Sciences
              </option>
              <option value="Faculty of Economics and Management Sciences"
                      <%= job.faculty === 'Faculty of Economics and Management Sciences' ? 'selected' : '' %>>
                Economics and Management Sciences
              </option>
              <option value="Faculty of Education"
                      <%= job.faculty === 'Faculty of Education' ? 'selected' : '' %>>
                Education
              </option>
              <option value="Faculty of Humanities"
                      <%= job.faculty === 'Faculty of Humanities' ? 'selected' : '' %>>
                Humanities
              </option>
              <option value="Faculty of Science and Technology"
                      <%= job.faculty === 'Faculty of Science and Technology' ? 'selected' : '' %>>
                Science and Technology
              </option>
            </select>
          </div>

          <div class="form-group">
            <label>Job Description:</label>
            <textarea name="description" required><%= job.description %></textarea>
          </div>

          <div class="form-group">
            <label>Requirements:</label>
            <textarea name="requirements" required><%= job.requirements %></textarea>
          </div>

          <div class="form-group">
            <label>Application Deadline:</label>
            <input type="date" name="application_deadline" 
                   value="<%= job.application_deadline %>" required>
          </div>

          <div class="form-group">
            <label>
              <input type="checkbox" name="is_active" <%= job.is_active ? 'checked' : '' %>>
              Job is active
            </label>
          </div>

          <button type="submit" class="btn">Update Job Post</button>
          <a href="/faculty/dashboard" class="btn btn-secondary" style="margin-left: 1rem;">Cancel</a>
        </form>
      </div>
    </div>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 University of Mpumalanga Job Connect. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
